#ifndef CONFIG_H
#define CONFIG_H

#define NODE_ID "Bananarama"

#define FB_SSID "Fallback SSID"     // Fallback WiFi SSID
#define FB_PASS "Fallback Password" // Fallback WiFi Password
#define FB_IP                       // Fallback IP address   
#define FB_MASK                     // Fallback netmask
#define FB_GW                       // Fallback gateway

#endif